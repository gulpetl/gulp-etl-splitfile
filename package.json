{
  "name": "gulp-datatube-streamsplitter",
  "version": "1.0.0",
  "description": "Splits a single vinyl object stream into multiple",
  "main": "dist/js/gulpfile.js",
  "typings": "dist/types/gulpfile.d.ts",
  "scripts": {
    "build-codeonly": "tsc && tsc --module commonjs --outDir dist/lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^10.12.18",
    "gulp": "^4.0.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.2"
  }
}
